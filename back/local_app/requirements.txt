# Зависимости сервиса зрительной модели. Отдельно от requirements.txt: тот
# ставится в контейнер, а этот — на хост, потому что Docker на macOS не отдаёт
# контейнеру ускоритель Apple, и модель обязана считаться снаружи.
#
# Версии закреплены те же, на которых модель гонялась в облаке: ответ должен
# совпадать, иначе поедет разбор.
torch==2.14.0
# Marlin построена на Qwen3-VL, и её видеопроцессор импортирует torchvision на
# этапе загрузки. Без него сервис падает с ImportError, до модели дело не доходит.
torchvision==0.29.0
torchcodec==0.16.0
transformers==5.16.1
accelerate==1.14.0
huggingface_hub==1.30.0
fastapi==0.141.1
uvicorn==0.52.4
